# Session State — 2026-05-04

## Что уже сделано

- Создана отдельная рабочая копия проекта:
  - `call-analyzer-codex`
- Основная папка `call-analyzer` не трогалась.
- Поднят локальный Streamlit-сайт:
  - `http://localhost:8501`
- Настроено локальное окружение:
  - `.venv`
- Добавлены:
  - `requirements.txt`
  - `.env.example`
  - `README.md`

## Архитектурная переделка

- Добавлено новое ядро V1:
  - `models_v1.py`
  - `rule_engine.py`
  - `jk_catalog.py`
  - `policy_config.py`
- Добавлены справочники:
  - `reference/jk_names.txt`
  - `reference/client_policy.json`
- Переделан `storage.py`:
  - теперь сохраняются отдельные артефакты:
    - `transcript.json`
    - `facts.json`
    - `report.json`
    - `metadata.json`
- Переделан `analyzer.py`:
  - раньше: один большой ответ модели
  - сейчас: `transcription -> fact extraction -> normalization -> rule-engine`
- Добавлен chunking длинных транскриптов с последующим слиянием фактов.

## Замена провайдера транскрибации

- Groq отключён как рабочий путь:
  - организация Groq заблокирована (`organization_restricted`)
- Транскрибация переведена на:
  - `OpenAI gpt-4o-mini-transcribe`
- Анализ по-прежнему идёт через:
  - `Anthropic`

## Что уже проверено

### Smoke-test 1

- Файл: `144627079.wav`
- Результат:
  - `call_status = out_of_funnel_callback_later_no_dialog`
  - `in_funnel = false`
  - `review_required = false`
- Вывод:
  - логика исключения из воронки работает

### Smoke-test 2

- Файл: `143980791.wav`
- Результат:
  - `call_status = in_funnel`
  - `overall_score = 40`
  - `review_required = true`
  - причина:
    - неуверенное распознавание ЖК
- Факты:
  - `meeting_offered = false`
  - `meeting_agreed = false`
  - `budget_discussed = true`
  - `next_step_discussed = true`
- Вывод:
  - архитектура работает на содержательном звонке
  - нужно точнее докрутить rule-engine для сценариев `перезвонить позже, но предметный диалог уже был`

## Что уже дополнительно исправлено

- Звонки вне воронки больше не показываются как `100/100`
- В `History` они отображаются как:
  - `Не оценивается`
- В `Report` и на главной странице это тоже учтено
- Во всех экранах выровнены новые статусы:
  - `Вне воронки: прерван до диалога`
  - `Вне воронки: обрыв после предметного диалога`
  - `Вне воронки: перезвонить позже без диалога`
  - `Вне воронки: нецелевой или сервисный звонок`

## Следующий логичный шаг

### Третья волна доработки

- Усилить rule-engine для сценариев:
  - `перезвонить позже`, но диалог уже предметный
  - `следующий шаг есть, встречи нет`
  - `контентный звонок без показа`
- Усилить нормализацию ЖК
- После этого сделать маленький батч:
  - `3–5 звонков`

## Что дополнительно сделано в продолжении

- Для сценария `перезвонить позже, но диалог уже был` rule-engine смягчён:
  - повторный согласованный звонок теперь не считается автоматически `passive_sale`
  - `meeting_required_not_done` не ставится автоматически, если звонок остановился на согласованном follow-up до презентации
  - этапы `Предложение альтернативы` и `Приглашение на встречу` могут считаться отложенными, а не проваленными, если клиент перенёс разговор до презентации
- Усилена нормализация ЖК:
  - добавлен `reference/jk_aliases.json`
  - `Еловск -> А101 Всеволожск`
  - `Лагово -> А101 Лаголово`
- Повторная оценка второго smoke-test проведена без API-затрат на основе сохранённых `facts.json`

## Актуальный статус второго smoke-test после донастройки

- Файл: `143980791.wav`
- После пересчёта по новым правилам:
  - `call_status = in_funnel`
  - `residential_complex = А101 Всеволожск`
  - `review_required = false`
  - `overall_score = 66`
  - `meeting_required_not_done = false`
  - `passive_sale = false`

## Что сделано после мини-батча и дополнительной калибровки

- Зафиксированы 2 продуктовых правила:
  - сервисный звонок по уже назначенному просмотру уходит в `out_of_funnel_non_target`
  - содержательный звонок, оборванный не по вине менеджера, уходит в `out_of_funnel_interrupted_after_dialog`
- В модель и policy добавлены признаки:
  - `existing_appointment_context`
  - `service_follow_up_question`
  - `interrupted_after_dialog_out_of_funnel`
  - `existing_appointment_service_calls_non_target`
- Rule-engine теперь различает:
  - настоящий обрыв после содержательного диалога
  - согласованный перенос разговора по занятости клиента
- Добавлен смысловой фильтр по встрече:
  - `обратный звонок / обращение в отдел продаж` больше не считается встречей
  - если есть только такой финал, звонок остаётся в воронке, но получает честный пропуск этапа встречи
- Все сохранённые `report.json` и `results/*.json` пересчитаны по сохранённым `facts.json` без новых API-затрат

## Актуальная картина после полной перекалибровки

- `143980791`
  - `call_status = in_funnel`
  - `overall_score = 66`
  - `residential_complex = А101 Всеволожск`
  - `meeting_required_not_done = false`
  - `passive_sale = false`
- `144071603`
  - `call_status = out_of_funnel_interrupted_after_dialog`
  - `review_required = false`
  - корректно вынесен из основной воронки как обрыв после уже начавшейся квалификации
- `144597284`
  - `call_status = out_of_funnel_non_target`
  - корректно классифицирован как сервисный звонок по уже существующей записи на просмотр
- `144628738`
  - `call_status = in_funnel`
  - `overall_score = 71`
  - `review_required = false`
  - `meeting_proposed = false`
  - `meeting_required_not_done = true`
  - `passive_sale = true`
  - то есть логика больше не считает “обратный звонок в отдел продаж” за приглашение на встречу
- `144597531`
  - `call_status = in_funnel`
  - `overall_score = 91`
  - `meeting_proposed = true`
  - `meeting_agreed = true`
  - подтверждён сильный сценарий продажи на длинном звонке

## Следующий шаг после текущего состояния

- Следующий разумный шаг:
  - прогнать следующий мини-батч на уже откалиброванной логике
  - посмотреть на более широкий набор кейсов:
    - реальные сервисные звонки
    - обрывы после квалификации
    - “скинуть материалы” без встречи
    - реальные приглашения на очную и онлайн-встречу

## Что уже сделано следующим шагом

- Прогнан второй мини-батч из 5 новых звонков:
  - `143944291`
  - `143946758`
  - `143981019`
  - `143983354`
  - `144002081`
- Подтверждены новые рабочие типы сценариев:
  - согласованная онлайн-встреча при занятом клиенте
  - встреча предложена, но не согласована
  - содержательный звонок с обрывом после квалификации
  - звонок с петлёй/повторами в транскрипте, который всё равно корректно уходит в статус исключения из основной воронки
- Детальная сводка сохранена в:
  - `BATCH_SMOKETEST_2026-05-04.md`
- Создан отдельный продуктовый чек-лист готовности:
  - `PRODUCT_READINESS_CHECKLIST.md`
- Создан отдельный roadmap:
  - `ROADMAP_NEXT_STEPS.md`
- Создан отдельный документ методологии:
  - `PRODUCT_METHODOLOGY.md`
- Создан отдельный журнал доказательств и прогресса:
  - `PROJECT_EVIDENCE_LOG.md`

## Замечание по рабочей папке

- В `git status` видны дополнительные файлы:
  - `analyzer 2.py`
  - `storage 2.py`
- Они не использовались в текущей переделке и не трогались автоматически.

## Важно

- Секреты уже лежат локально в `.env`
- Не коммитить `.env`
- При необходимости после завершения настройки можно ротировать API-ключи
- Проект уже ведётся не как “разовый MVP”, а как заготовка под серьёзный B2B-продукт с product-readiness подходом

## Что сделано 2026-05-05 на первой партии полного батча A101

- Запущена и завершена первая партия из `8` новых звонков:
  - `144002430`
  - `144002651`
  - `144004465`
  - `144034179`
  - `144034363`
  - `144034381`
  - `144119035`
  - `144119806`
- Все 8 звонков обработаны через:
  - `OpenAI gpt-4o-mini-transcribe`
  - `Anthropic` extraction
  - текущий `rule_engine.py`
- После прогона выполнена дополнительная калибровка без новых API-затрат:
  - звонки пересчитаны по сохранённым `facts.json`

## Что именно докручено по результатам этой партии

- Исправлен сценарий пустого контакта:
  - если в звонке есть только приветствие менеджера и дальше нет содержательного диалога, такой звонок уходит в `out_of_funnel_interrupted`, а не в `in_funnel`
- Исправлен сценарий согласованной встречи при плохой связи:
  - если синхронный следующий шаг уже согласован, технические помехи или плохая связь в конце разговора не выбрасывают звонок из основной воронки

## Ключевые результаты по аномальным кейсам

- `144119035`
  - раньше после первого пересчёта ошибочно был `in_funnel`
  - после донастройки стал:
    - `out_of_funnel_interrupted`
- `144119806`
  - раньше ошибочно уходил в:
    - `out_of_funnel_interrupted_after_dialog`
  - после донастройки стал:
    - `in_funnel`
    - `meeting_proposed = true`
    - `meeting_agreed = true`

## Что делать следующим шагом

- Сформировать shortlist звонков для вашей ручной экспертной валидации:
  - не все подряд
  - а только спорные и показательные кейсы
- После этого:
  - запускать следующую партию оставшихся звонков A101

## Что дополнительно сделано после ручной валидации 2026-05-05

- Создан файл ручных вердиктов:
  - `A101_VALIDATION_VERDICTS_2026-05-05.md`
- Уточнён алиас ЖК:
  - `Кеновск -> А101 Всеволожск`
- Исправлена техническая недоработка в нормализации:
  - `jk_aliases.json` теперь реально используется в `analyzer.py` при сборке каталога ЖК
- После этого пересчитан звонок `144034179` по сохранённым `facts.json` без новых API-затрат:
  - `residential_complex = А101 Всеволожск`
  - `review_required = false`

## Какие 2 продуктовых вопроса открылись после вашей ручной проверки

- `Формально согласован следующий шаг, но операционное подтверждение шаткое`
  - пример: `144119806`
- `У клиента нет подходящего продуктового решения у застройщика`
  - пример: `144034179`

## Что добавлено после этого решения

- Введён отдельный слой экспертных override’ов:
  - `reference/expert_overrides.json`
  - `expert_overrides.py`
- Override-слой уже подключён к:
  - `pages/report.py`
  - `pages/history.py`
- На практике это уже используется для:
  - ручного подтверждения встреч
  - ручного снятия `review_required`, если ЖК подтверждён экспертом
  - исключения звонка из `оценочного резерва`, если у застройщика нет подходящего продуктового решения

## Что сделано следующим шагом после этого

- Введён новый продуктовый слой:
  - `встреча согласована по разговору`
  - `встреча операционно подтверждена`
- Это различие теперь встроено в:
  - `rule_engine.py`
  - `models_v1.py`
  - `app.py`
  - `pages/history.py`
  - `pages/report.py`
- Дополнительно устранён перекос в хранении:
  - `storage.load_all_results()` теперь возвращает только последнюю актуальную версию результата на каждый `call_id`, а не накапливает дубли после пересчётов

## Что сделано после этого на следующей партии A101

- Прогнана ещё одна партия из `8` звонков:
  - `144119900`
  - `144121459`
  - `144146187`
  - `144522817`
  - `144524928`
  - `144559005`
  - `144561448`
  - `144596252`
- Создан отдельный shortlist на ручную валидацию:
  - `A101_MANUAL_VALIDATION_SHORTLIST_BATCH2_2026-05-05.md`
- На этой партии дополнительно подтвердились:
  - подтверждённая очная встреча
  - звонок с предложенной, но не согласованной встречей при сильной речевой обработке
  - несколько корректных исключений из KPI из-за технических обрывов

## Что изменилось после ручной валидации партии 2

- `144119900`
  - переведён в `out_of_funnel_interrupted`
  - не считается негативным звонком для менеджера
- `144121459`
  - подтверждён как `А101 Лаголово`
  - встреча согласована и операционно подтверждена
- `144146187`
  - закреплён как кейс `встреча предложена, но не согласована`
  - добавлена экспертная пометка о долгосрочном и осторожном клиенте
- `144522817`
  - закреплён как качественный звонок без согласованной встречи
- `144559005`
  - переведён в согласованную и операционно подтверждённую онлайн-встречу
- `144596252`
  - переведён в согласованную и операционно подтверждённую очную встречу
- `144524928`
  - подтверждён как корректное исключение из KPI из-за плохой связи
- `144561448`
  - подтверждён как корректное исключение из KPI без негативной оценки менеджера

## Что сделано следующим шагом после этого

- Отчёт перестроен под новый слой зрелости:
  - `мягкий резерв`
  - `жёсткий резерв`
  - `клиент пока не готов`
- Эта логика теперь живёт в:
  - `pages/report.py`
  - `pages/history.py`
  - `app.py`
- Новый класс `клиент пока не готов` больше не смешивается с прямой недоработкой менеджера и не считается резервом один в один

## Что сделано после этого на финальной партии A101

- Прогнана последняя партия из `8` звонков:
  - `144596729`
  - `144596918`
  - `144626767`
  - `144626828`
  - `144637616`
  - `144648297`
  - `144648992`
  - `144649116`
- Создан shortlist на ручную проверку:
  - `A101_MANUAL_VALIDATION_SHORTLIST_BATCH3_2026-05-05.md`

## Текущая полная картина по A101

- Обработано всего:
  - `35` звонков из `35`
- Из них:
  - `23` в основной воронке
  - `12` встреч согласованы по разговору
  - `10` встреч операционно подтверждены
  - `7` кейсов `клиент пока не готов`
  - `1` кейс `мягкого резерва`
  - `1` кейс `жёсткого резерва`
  - `5` звонков требуют экспертной верификации

## Что изменилось после ручной валидации партии 3

- `144596918`
  - переведён из `жёсткого резерва` во `внешне сорванный содержательный звонок`
  - попытка менеджера позвать клиента на встречу засчитана как положительная
- `144626767`
  - подтверждён как `А101 Всеволожск`
  - подтверждённая очная встреча, `review_required` снят
- `144626828`
  - закреплён как кейс `клиент пока не готов`, а не потеря встречи
- `144648297`
  - переведён в `in_funnel`
  - закреплён как `клиент пока не готов`, а не обрыв или мягкий резерв
- `144649116`
  - подтверждён как `А101 Лаголово`
  - `review_required` снят
- `144596729`
  - переведён из `out_of_funnel_interrupted_after_dialog` в `in_funnel`
  - закреплён как `клиент пока не готов`
- `144637616`
  - переведён в `out_of_funnel_non_target`
  - закреплён как кейс `product mismatch / некорректный перевод`
- `144648992`
  - переведён из `клиент пока не готов` в подтверждённую встречу

## Что это значит для следующего этапа

- Первый застройщик теперь закрыт не частично, а полным раундом
- Следующий зрелый шаг проекта:
  - ручная валидация спорных звонков партии 3
  - финальная донастройка по A101
  - затем переход к новым застройщикам на уже намного более устойчивой методологии

## Что автоматизировано после этого

- Правило `клиент пока не готов` больше не держится только на ручных override:
  - если встреча реально предложена
  - клиент её не согласовал
  - и уходит в сценарий `пришлите / я сначала сам(а) изучу`
  - кейс автоматически уходит в `client_not_ready`
- Правило `некорректный перевод / product mismatch` усилено на уровне fact extraction:
  - такие звонки должны автоматически тянуться в `out_of_funnel_non_target`
- Анализ-версия повышена до `v1-beta`
- `expert_overrides.py` теперь автоматически пересчитывает старые звонки по `facts.json`, если версия анализа устарела

## Актуальная картина после автоматизации `v1-beta`

- `35` звонков обработаны
- `23` в основной воронке
- `12` встреч согласованы по разговору
- `10` встреч операционно подтверждены
- `8` кейсов `клиент пока не готов`
- `0` кейсов `мягкого резерва` на текущем A101
- `1` кейс `жёсткого резерва`
- `5` звонков требуют экспертной верификации
