# Session State — 2026-05-07

## Что зафиксировано

Сегодня был завершён внешний цикл согласования между Codex и Cloud Code по архитектуре, backlog и приоритетам развития продукта.

Общий итог:

- финальный backlog согласован
- внешний ревьюер подтвердил, что общий знаменатель достигнут
- дополнительные скрытые риски внесены в backlog
- новые раунды согласований завершены
- следующий шаг — не обсуждение, а реальные правки в `call-analyzer-codex`

## Где лежит согласованный пакет для Cloud Code

Папка:

- `/Users/nikitaoreshenkov/Documents/New project/кодекс для Cloud-кода`

Ключевые итоговые файлы:

- `04_FINAL_CONSENSUS.md`
- `05_BACKLOG_FINAL.md`

## Что подтверждено Cloud Code

Cloud Code принял:

- финальный backlog как достаточный и зрелый
- завершение этапа согласований
- переход к реализации

Подтверждённый стартовый порядок реализации:

1. Блок `Сделать сейчас`, пункты `1–4`, одним первым инженерным проходом:
   - `n_chunks` вместо ложной семантики `was_truncated`
   - убрать двойной `load_all_results()`
   - вынести `CALL_STATUS_LABELS` в одно место
   - добавить `@st.cache_data` на тяжёлые чтения
2. Следом — пункт `5`:
   - regression-fixture по `A101`

## Актуальная финальная версия backlog

Финальный backlog зафиксирован в:

- `/Users/nikitaoreshenkov/Documents/New project/кодекс для Cloud-кода/05_BACKLOG_FINAL.md`

Последние принятые уточнения:

- `score_applicable` поднят в блок `Сделать сейчас`
- regression-fixture по `A101` должен включать `expected_call_status`
- `passive_sale` vs `meeting_required_not_done`:
  - не чинить через `xor` в `rule_engine`
  - сначала проверить реальное пересечение на данных
  - затем править клиентский рендер
- `_normalize_missed_text` должен получить unit-тесты до второго застройщика
- стратегия `_pick_longest` для `call_summary` должна быть пересмотрена до второго застройщика
- guard для `review_required` по `meeting_offered.value is None` нужен только для `IN_FUNNEL`

## Что делать завтра

Работать уже в основной живой папке:

- `/Users/nikitaoreshenkov/Documents/New project/call-analyzer-codex`

Начинать с реализации:

1. `n_chunks`
2. один источник `CALL_STATUS_LABELS`
3. убрать двойной `load_all_results()`
4. добавить `@st.cache_data`
5. потом готовить regression-fixture по `A101`

## Что не нужно делать завтра

- не запускать новые раунды согласований с Cloud Code
- не возвращаться к обсуждению финальности backlog
- не трогать handoff-пакет без необходимости
- не прогонять новые звонки до первого инженерного блока правок

## Короткий статус

Backlog согласован. Переходим в режим реализации.

## Что уже реализовано после согласования

Сделано в живой рабочей папке `call-analyzer-codex`:

1. Первый инженерный блок закрыт:
   - добавлен `n_chunks` вместо ложной семантики `was_truncated`
   - убран двойной `load_all_results()` в `pages/report.py`
   - `CALL_STATUS_LABELS` вынесены в единый файл `constants.py`
   - добавлено кэширование тяжёлых чтений в `pages/history.py` и `pages/report.py`

2. Собран контрольный эталонный набор `A101`:
   - файл `reference/a101_regression_fixture.json`
   - автопроверка `tests/test_a101_regression.py`
   - включает `expected_call_status` и ключевые флаги

3. Закрыт блок честного отображения оценки:
   - добавлен `score_applicable`
   - out-of-funnel звонки явно помечаются как `Не оценивается`

4. Убран лишний шум `review_required`:
   - проверка факта встречи больше не поднимается автоматически для звонков вне воронки

5. Проверено пересечение `passive_sale` и `meeting_required_not_done` на реальных данных `A101`:
   - пересечение есть ровно в 1 звонке
   - дублирование убрано на уровне клиентского рендера

6. Исправлен учёт `ClientPolicy.valid_meeting_types`:
   - система теперь различает “встреча вообще предложена” и “предложен допустимый формат встречи”
   - если для клиента валидна только очная встреча, одна онлайн-встреча больше не считается корректным результатом
   - если предложены оба формата, а хотя бы один из них допустим, встреча засчитывается
   - добавлены автотесты `tests/test_rule_engine_policy.py`

7. Дополнительная защита клиентского HTML уже на месте:
   - модуль `report_text.py`
   - тесты `tests/test_report_text.py`

8. Начато и внедрено разделение данных по клиентам:
   - добавлен модуль `client_paths.py`
   - `results`, `artifacts` и клиентские справочники теперь могут жить отдельно по `client_id`
   - в `app.py`, `pages/history.py` и `pages/report.py` добавен выбор `ID клиента`
   - для текущего `A101` сохранена мягкая совместимость со старой раскладкой через `default`
   - это позволяет дальше подключать второго застройщика без смешивания данных в одной общей папке

9. Добавлен шаблон нового клиента:
   - модуль `client_setup.py`
   - в интерфейсе появилась кнопка `Подготовить шаблон клиента`
   - для нового клиента автоматически создаются:
     - `reference/<client_id>/client_policy.json`
     - `reference/<client_id>/jk_names.txt`
     - `reference/<client_id>/jk_aliases.json`
     - `reference/<client_id>/expert_overrides.json`
     - `reference/<client_id>/README.md`
     - `results/<client_id>/`
     - `artifacts/<client_id>/`
   - добавлен автотест `tests/test_client_setup.py`

Все актуальные автопроверки на этом этапе проходят.
